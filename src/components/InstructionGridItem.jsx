import Grid from '@mui/material/Grid'
import { styled } from '@mui/material/styles'

const InstructionGridItem = styled(Grid)({
  color: '#343a40',
  textAlign: 'center',
  fontSize: '1rem',
  height: '8vh',
  fontWeight: 'bold'
})

export default InstructionGridItem
